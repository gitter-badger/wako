import { ScrollWatcherDirective } from './scroll-watcher.directive';

describe('ScrollWatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollWatcherDirective();
    expect(directive).toBeTruthy();
  });
});
