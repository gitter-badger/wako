export class Settings {
  debug = {
    enabled: false,
    logLevel: 'error'
  };
  showNotifications = {
    enabled: true,
    enableOnlyIfUptToDate: true // If user has watched all the aired episode
  };
}
